# WSL build tree lives outside repo at ~/rtems — nothing to ignore there.

# Generated boot artifacts (rebuild with LeapGateway-linux/alpine/mk-image.sh)
rtems-image/leapos-gateway-alpine.img

# Editor / OS
.DS_Store
Thumbs.db
*.swp
