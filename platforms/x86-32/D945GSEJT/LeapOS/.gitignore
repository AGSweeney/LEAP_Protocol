# WSL build tree lives outside repo at ~/rtems — nothing to ignore there.

# Generated boot artifacts (rebuild with rtems-build/*.sh)
rtems-image/leapos-device.img
rtems-image/leapos-gateway.img
rtems-image/staging-device/
rtems-image/staging-gateway/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
